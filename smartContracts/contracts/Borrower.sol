// SPDX-License-Identifier: UNLICENSED

pragma solidity ^0.8.0;

/// @notice This contract defines the logic for the person who wants a loan. They 
///         must define collateral (e.g. an NFT that represents ownership of something).

import "@openzeppelin/contracts/access/Ownable.sol";

contract Borrower {

    // event AskedLoan(address borrower, uint256 amount);


}